# FrameIntro
Local IMDb is a Java Swing–based desktop application that allows users to store and search actor movie information locally. It uses basic GUI components, object-oriented programming, and file handling to save and retrieve actor names and movie release years from a text file.
